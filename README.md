# HVAC v3.1 Final Deployment Bundle

Open `README_DEPLOYMENT.md` for full instructions.

Main Streamlit file:

```text
streamlit_app.py
```

Local run:

```bash
pip install -r requirements.txt
streamlit run streamlit_app.py
```

CLI run:

```bash
python run_hvac_v31_auto.py
```

Use `energy_kwh_v31_final` as the clean calibrated baseline for the next S0–S3 and Severity × Strategy simulations.
